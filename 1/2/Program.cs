/*Console.Write("Введите число: ");
int number = int.Parse(Console.ReadLine());
int[] array = new int[10];

void FillArray(int[] array);
{
    random randGeneretor = new random();
     for (int i = 0; i < array.Length; i++)
    {
        array[i] = randGenerator.Next(0, 10);
    }
}
*/
