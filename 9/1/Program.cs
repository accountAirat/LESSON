string[] alphavid = {"а", "и", "с", "в"};
int size = int.Parse(Console.ReadLine()!);
string[] combinations = new string[size];
for (int i = 0; i < size; i++)
{
    for (int i = 0; i < alphavid.Length; i++)
    {
        for (int i = 0; i < alphavid.Length; i++)
        {
            
        }
    }
}
